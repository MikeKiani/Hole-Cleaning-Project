# Core scientific stack for the hole-cleaning ML workflow.
# Pinned to lower bounds known to work; newer versions are fine.
numpy>=1.23
pandas>=1.5
scikit-learn>=1.3
scipy>=1.9
matplotlib>=3.6
seaborn>=0.12
joblib>=1.2

# Optional — only needed to run the walkthrough notebook.
# jupyter>=1.0
# ipykernel>=6.0
